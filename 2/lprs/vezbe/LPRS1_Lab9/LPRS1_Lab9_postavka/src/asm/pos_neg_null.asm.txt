
/*
	Ekvivalentan C kod:

	int N = 10;
	short a[10] = {-3, 0, 0, 7, 12, -6, 15, -1, 0, 11};
*/
.data
10	// N = 10, adresa 0
-3	// a[0], adresa 1
0 	// a[1], adresa 2
0 	// a[2], adresa 3
7 	// a[3], adresa 4
12 	// a[4], adresa 5
-6 	// a[5], adresa 6
15 	// a[6], adresa 7
-1 	// a[7], adresa 8
0 	// a[8], adresa 9
11 	// a[9], adresa 10

/*
	Ekvivalentan C kod (viši nivo):
	
	for(i=0; i<N; i++)
	{
		R1 = a[i];
		if(R1 == 0)
			R5++;
		elsif(R1 < 0)
			R6++;
		else
			R7++;
	}
*/
.text
/*
	Spisak registara:

	R0 - i - adresa elementa iz memorije.
	R1 - učitan element iz memorije a[i]
	R3 - N = 10
	R5 - broj NULA
	R6 - broj negativnih
	R7 - broj pozitivnih
*/
begin:
	ld R3, R0;	// ucitan broj 10 (sa adrese 0) u R3

petlja:
	sub R2, R3, R0;
	jmpz end;	

	inc R0, R0;	// i (adresa) pokazuje na prvi element -> -3
	ld R1, R0; 	// R1 = a[i]
	jmpz nule;
	jmps minus;
	// onda je pozitivan
	inc R7, R7;
	jmp petlja;
nule:
	inc R5, R5;
	jmp petlja;
minus:
	inc R6, R6;
	jmp petlja;	
	
end:
	jmp end // Beskonačna petlja.

